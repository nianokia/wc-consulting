import React from "react";

// PURPOSE - display a footer at the end of every page with the name of the company
// its a reusable component that can be called in many components while being editted in one component (normalizes code)

const Footer = () => {
  return (
    <>
      <footer>Wright Choice Consulting</footer>
    </>
  )
}

export default Footer